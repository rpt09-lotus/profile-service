config:
  target: 'http://localhost:3002'
  phases:
    - duration: 20
      arrivalRate: 105
  defaults:
    headers:
      x-my-service-auth: 'foo'
scenarios:
  - flow:
    - get:
        url: "/9500000"
